repos:
-   repo: https://github.com/pre-commit/mirrors-autopep8
    rev: v2.0.2
    hooks:
    -   id: autopep8
        args:
        - -i
        - --max-line-length=90
        # Vendored launcher kept verbatim; ruff also excludes it.
        exclude: ^tools/startme/startme\.py$
